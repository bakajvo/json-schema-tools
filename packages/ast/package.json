{
  "name": "@jsonschema-tools/ast",
  "version": "1.0.0",
  "description": "",
  "main": "src/index.ts",
  "files": [
    "dist"
  ],
  "sideEffects": false,
  "scripts": {
    "build": "tsup src --format esm,cjs --dts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "tsup": {
    "clean": true,
    "target": "es2019",
    "format": [
      "cjs",
      "esm"
    ]
  },
  "devDependencies": {
    "tsup": "^6.6.3"
  }
}
